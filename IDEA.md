# 记录一些想法

## 20260919
1.附加模块
- 内置模块系统：主程序提供统一模块接口，每个模块实现这个对应的接口，加载时校验版本和完整性
- 模块目录检测：主程序启动时扫描
```text
/etc/nixos/opnixmgr/modules/
```
存在就可选择加载这个模块
- 模块校验：加载前检查入口文件，接口版本，依赖是否满足，不通过就忽略，不阻止主程序启动
- 在线模块商店(概念)：主程序内置模块商店，从远程仓库下载模块列表(md文件)，用户下载安装，自动校验并启用
- 模块安全：主模块限制子模块权限

2.源代码模块
- 代码模块化设计：主程序main.cpp，只负责调用，每个逻辑做成独立的hpp+cpp
- 头文件声明，源代码实现，主程序调用不实现
- 目前hpp和cpp都放在 src/ 即可
- 后面如果有需要可以拆成 include/ 和 src/

## 20260920
1. 全新命名体系
- 原来是UT系列为我本人写核心AI写外壳，不带UT为纯AI写的(现已废除)
- 现在体系为：主要为C++语言写的就属于OP系列，主要为Rust写的属于RS系列，剩下的其他语言则属于FS系列
- 原来概念级别的UTLinux安全模型项目更名为Sakuya属于独立系列(目前Sakuya系列处于保密阶段)
- 还有Sakuya的扩展程序则名为Sakuya-Extension-XXX
- 若不属于Sakuya系列的系列内有一个Sakuya需要的组件，则可独立使用的组件名字不变，接入Sakuya扩展系统的则名字后面带"For-Sakuya"，组件若接入Sakuya扩展系统则组件必须有Sakuya框架才可以运行

### 关于系列命名？
1. UT系列源于我的其中一个网名"UpwindTrail"，也就是"逆风轨迹"
2. OP系列源于我的一个曾用网名"OP432Hz"，代表一个全新的开始
3. 关于RS和FS？
- RS其实就是"Reimilia Scarlet"的缩写
- FS就是"Flandre Scarlet"的缩写
4. Sakuya其实就就是"十六夜咲夜"的英文名

### Reimilia项目部署安装器
1. 核心逻辑：读取远程仓库根目录的/repo/xxx.md文件，每个md文件都包含了项目地址，根据项目地址读取那个仓库根目录下的md文件获取安装配置进行自动安装
2. 项目属于独立系列

## Sakuya的真实定位
1. Sakuya只是一个插件通信框架，不是一个功能，且可以跨发行版不强制绑定NixOS
2. 插件是单文件，后缀为".sakuyamodule"
3. 在线商店逻辑与Reimilia一致
4. 若一个项目同时支持独立使用和Sakuya通信则每个都属于各自独立的版本
- 独立版：使用命令行配置
- Sakuya插件版：可通过Sakuya框架显示图形配置界面，且可以与其他组件通信实现效果
- 冲突检测：二者只能选一个安装。若安装了Sakuya版则独立版无法安装，若安装了独立版则Sakuya会报错退出(安装Sakuya版需要在Sakuya内安装)
